describe('offlineSyncCourse', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(offlineSyncCourse) {

	//expect(offlineSyncCourse.doSomething()).toEqual('something');

  }));

});