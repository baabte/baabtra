describe('companyRegistration', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(companyRegistration) {

	//expect(companyRegistration.doSomething()).toEqual('something');

  }));

});