describe('addUserMenu', function() {

	beforeEach(module('baabtra'));

	it('should ...', inject(function($controller) {

        var controller = $controller('addUserMenu');

		expect(controller('input')).toEqual('output');

	}));

});