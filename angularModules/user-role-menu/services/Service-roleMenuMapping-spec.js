describe('roleMenuMapping', function() {

  beforeEach(module('baabtra'));

  it('should ...', inject(function(roleMenuMapping) {

	//expect(roleMenuMapping.doSomething()).toEqual('something');

  }));

});